module_id: test.circular_b
description: Circular reference test (part B)
version: "1.0.0"

definitions:
  TypeB:
    type: object
    properties:
      value:
        type: integer
      ref_to_a:
        $ref: "./circular_a.schema.yaml#/definitions/TypeA"

input_schema:
  type: object
  properties:
    data:
      $ref: "#/definitions/TypeB"

output_schema:
  type: object
  properties:
    status:
      type: string
