module_id: test.circular_a
description: Circular reference test (part A)
version: "1.0.0"

definitions:
  TypeA:
    type: object
    properties:
      name:
        type: string
      ref_to_b:
        $ref: "./circular_b.schema.yaml#/definitions/TypeB"

input_schema:
  type: object
  properties:
    data:
      $ref: "#/definitions/TypeA"

output_schema:
  type: object
  properties:
    status:
      type: string
